Code: To run the code (Jupyter Notebook), install all the libraries and packages as mentioned in the report in the reference section and run all the cells sequentially.
Data: Required data files are provided in the zip.
